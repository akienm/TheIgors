{
  "_comment": "Shared project settings. MCP config is machine-specific — run claudecode/bootstrap_cc.sh (Linux) or claudecode/bootstrap_cc.ps1 (Windows) to generate .claude/settings.local.json with correct paths."
}
