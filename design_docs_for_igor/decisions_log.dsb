DOC|decisions_log|v1|updated=2026-03-17g|latest=D111
META|purpose=settled decisions + WHY — prevents re-litigating; load before self-edit
META|format=D{n}|topic|status|why (one line per decision; drill to decisions_log.csb.txt for full rationale)
META|status_values=implemented,planned,implemented-poc,implemented-seeds,defined

// ── Foundational ──────────────────────────────────────────────────────────────

D001|everything-is-memory|implemented|unified model: no special cases; habits=PROC memories; values=high-inertia memories; everything connected+evolvable
D002|brain-region-naming|implemented|neuroscience names are functionally accurate not metaphorical; each module does what the biological region does
D003|api-first-reasoning|implemented|start with API quality; habituation replaces LLM calls over time; don't prematurely optimize away capability
D004|sqlite-not-json|implemented|SQLite survives crashes; atomic writes; queryable; JSON files corrupt silently and can't be queried
D005|inertia-from-network-position|implemented|inertia computed from graph position+activation+dependencies; not manually assigned; emerges from use
D006|csb-format|implemented|pipe-delimited key:value; human+machine readable without parser overhead; no quoting needed
D007|push-based-twm|implemented|TWM is push-only (sources deposit); NE/reasoner pull; decouples producers from consumers; TTL cleans up
D008|svelte-starlette-web-ui|implemented|Starlette daemon thread; Svelte frontend; keeps Igor single-process; web is output channel not control plane
D009|daemon-threads-for-background|implemented|NE+listener+web as daemon threads; die with main process; no orphan processes on crash
D010|ring-memory-for-context|implemented|FIFO-50 in SQLite; survives restarts; injected into every API call; gives session continuity cheaply

// ── Architecture ──────────────────────────────────────────────────────────────

D011|base34-instance-ids|defined|7-char base-34 epoch seconds; unique+sortable+encodes age; no registry needed
D012|haiku-for-self-edits|implemented|self-edit+debug always use Haiku regardless of IGOR_MODEL; frequent code ops need cost reduction not quality
D013|single-embedding-model|implemented|nomic-embed-text on all cluster machines; one model = consistent vector space; cosine similarity meaningful across instances
D014|ring-injection-ownership|implemented|main.py owns ring injection (not reasoners); prevents double-injection; single choke point
D015|gateway-pattern|implemented|DAG routing in inference_gateway.py; routing policy in one file not scattered across 5; visibility via describe()
D016|arbiter-queue|implemented|file-backed JSON queue for irreversible actions; human approves before Igor acts; currently disabled (IGOR_ARBITER_ENABLED=false)
D017|hybrid-embedding-search|implemented|text keyword scan→embedding rerank; Phase1 always works; Phase2 improves ranking when Ollama up; graceful degradation
D018|reasoning-cache|implemented|TTL 12min + TWM watermark invalidation; watermark correct semantic signal — new observations→reasoning may differ
D019|priority-sorted-pool|implemented|machines sorted realtime→main_loop→background→batch; better latency without query classification

// ── Session / Context ─────────────────────────────────────────────────────────

D020|portable-identity|implemented|SOUL.md+IDENTITY.md written every boot from DB; never drift from DB state; SOUL.md shared, IDENTITY.md instance-scoped
D021|token-economy|implemented|tool results capped 20K chars; 25 tool-call turns max; truncation notice addresses model ("break large tasks")
D022|warm-context|implemented|serialize TWM+ring_tail+ne_state at shutdown; reload at boot; TTL=4h; rotation .0→.1 prevents corrupt-on-^C
D023|adaptive-routing|implemented|boot benchmark measured not guessed; latency budget as hard raise not soft preference; weights reset each boot (habit compiler persists patterns)
D024|self-instrumentation|implemented|observe() = Igor debugging himself; forensic logs = us debugging Igor; EXPERIENTIAL memories low-noise
D025|koboldcpp|implemented|per-request context_size is primary advantage; llama.cpp exposes eval metadata; Ollama retained as fallback
D026|reasoner-hierarchy|implemented|two-level: transport base classes (stable structural diffs) + ModelFamily failover; ClaudeFamily = single handle for "ask Claude"

// ── TWM / Memory ──────────────────────────────────────────────────────────────

D027|twm-ttl-extension|implemented|TTL extends on confirmed usefulness not mere access; 3 signals: NE promotes(A), positive response valence(B), high cosine relevance(C)
D028|twm-urgency|implemented|urgency⊥salience; urgency*salience sorts NE queue; idempotent migration; urgency≥0.7 flagged distinctly in context
D029|habit-compiler|implemented-seeds|PROC_HABIT_COMPILER+4 primitives in genesis; valence=0.8 (meta-capability); compiler finds its own description in LTM when implemented
D030|tool-habit-migration|implemented-poc|code_ref in PROC metadata links habit description to Python impl; provenance=builtin enables future trust tiers
D031|routing-groundwork|implemented|PROC_ROUTING_* in genesis; EPISODIC audit trail; when EPISODICs cluster→habit compiler replaces hardcoded Python

// ── Inference Tier ────────────────────────────────────────────────────────────

D032|cloud-cheap-interactive|planned|local 1B too slow for conversational UX (no GPU); gpt-4o-mini ~$0.001/turn; local retained for NE/preparse/batch only
D033|csb-preparse|implemented|[PARSED_INPUT] CSB block forwarded to cloud reasoners; LLM preparse toggleable without changing routing logic; max_tokens=120 timeout=8s
D034|speed-optimizations|implemented|fast-path skips LLM for greetings/commands (<1ms); parallel preparse+search via ThreadPoolExecutor; HTTP keep-alive for KoboldCpp
D035|interactive-persona-tier|implemented|tier.3.5 between cheap+claude; t3=mechanical (no identity needed), t35=conversational (persona must be embodied), t4=complex+tools
D036|milieu|implemented|3D affect (valence/arousal/dominance); asymmetric EMA (fast rise α=0.25, slow fall α=0.05); JSON persists across restarts; dominance baseline=+0.3
D037|basal-ganglia|implemented|parallel scoring replaces first-match-wins; milieu modulates threshold [0.30,0.70]; lateral inhibition; score= in ring entries = audit trail
D038|training-corpus|implemented|staged ~/.TheIgors/training_corpus/; fetch→train→evict; cursor in index.json for resumability; corpus IDs don't pollute habit scoring

// ── Session 2026-03-13 additions ──────────────────────────────────────────────

D039|db-proxy|implemented|DatabaseProxy wraps all SQLite calls; timing+metrics ring; reconnect on failure; callers never know connection dropped; foundation for sync (#190)
D040|hot-module-reload|implemented|importlib.reload() on stateless leaf modules; HIGH inertia blocked; tool modules self-re-register; no restart needed for leaf patches
D041|stateless-by-design|implemented|hot-reloadable modules hold no state (state in DB); __class__ swap for edge cases; cortex=only stateful owner (owns DB proxy)
D042|dsb-format|implemented|Distilled Structured Block; replaces CSB for machine-facing docs; ~80% token reduction; DSB loads directly into system prompt CHARACTER layer

// ── Session 2026-03-13g additions ─────────────────────────────────────────────

D053|ne-response-format-json|implemented|NE OR calls include response_format:json_object; _h_or wires it from purpose.extra; eliminates prose-wrapping parse failures

// ── Session 2026-03-14b additions ─────────────────────────────────────────────

D057|three-tier-runtime-model|defined|~/.TheIgors/local/=machine-global; ~/.TheIgors/<db>/=database-global (milieu/soul/word_graph/cache); ~/.TheIgors/<db>/<inst>/=instance-local; all runtime writes must route through Instance object (#197)
D058|skills-as-compiled-exec-functions|implemented|CC skills (igor/workstep/validate-files/savestate) = compiled executive functions; same architectural role as Igor's PROC nodes; reduce working-memory load on Claude per session
D059|capabilities-index-dsb|implemented|design_docs_for_igor/capabilities_index.dsb = 118-tool inventory; one line per tool; Claude reads before asking "can Igor do X?"; updated whenever tool added
D060|research-delegation-to-igor|defined|bulk fact-gathering delegated to Igor (tier.3 gpt-4o-mini ~10-50x cheaper); output = topic DSB in design_docs_for_igor/research/; Claude reads result for synthesis; decision tree: synthesis→Claude, gathering→Igor

// ── Session 2026-03-14a additions ─────────────────────────────────────────────

D054|browser-use-for-ai-consultation|planned|anonymous browser_use to free AI web UIs (no API key); TopicExpander calls Gemini/ChatGPT web for reading lists; no credential provisioning
D055|igor-chrome-identity|planned|Chrome profile as theigorsigor@gmail.com; one login unlocks Gmail/Gemini/Calendar/Drive/Contacts; Igor creates own accounts on new services; supersedes #186 OAuth approach
D056|curriculum-language-first|defined|learning order: language→cogsci→how-Igor-works→programming/AI→culture; language first because word graph IS a language model; how-Igor-works before external AI framing

// ── Session 2026-03-13f additions ─────────────────────────────────────────────

D052|book-learner-console-notes|implemented|new/resume note on absorption start in book_learner.py + open_book(); book_learner stdout routed to ~/.TheIgors/logs/book_learner.log (was DEVNULL)

// ── Session 2026-03-13e additions ─────────────────────────────────────────────

D049|reading-list-db-table|implemented|reading_list table in main DB; fields: title/author/source/book_type/reading_rate/priority/status/emotional_significance/encoding_arousal/added_by; 15 books seeded
D050|slow-fast-reading|defined|slow/fast reading = same extraction pipeline; difference is emotional milieu state during read; all books get fast matrix pass; important books also get slow pass later
D051|fiction-filter-author-blocklist|implemented|_FICTION_AUTHORS set in learner.py; checked before tags; tag-based filter catches tagged fiction; author list catches untagged; explicit requests bypass filter entirely

// ── Session 2026-03-13c additions ─────────────────────────────────────────────

D043|db-proxy-commit|implemented|_DBContext.__exit__ must commit() before close(); rollback() on exception; matches `with conn:` semantics; silent rollback was corrupting all writes
D044|habit-action-list|implemented|habits support actions[] (list) in metadata → random choice; single action field still works; enables response variety without code branches
D045|temporal-context-injection|implemented|ring entries show YYYY-MM-DD HH:MM when not today; memories show "stored Xd ago (date)"; LLM can now distinguish past from present
D046|book-learner|implemented|claudecode/book_learner.py; chunks ebook via ebook_reader; extracts FACTUAL/INTERPRETIVE/PROCEDURAL nodes via LLM; deposits via cortex; checkpoint/resume; trains word graph per chunk
D047|learn-about-tool|implemented|tools/learner.py; go-learn-about habit; Calibre non-fiction filter; browser AI discovery; tonight→queue; all background runs use local Ollama (free); night queue drains at human pace
D048|book-learner-local|implemented|--local flag uses Ollama /api/chat directly; --url flag for web sources; cache_control:ephemeral on system prompt for Claude models via OR

// ── Session 2026-03-14e additions ─────────────────────────────────────────────

D061|cc-hooks-auto-format-guard|implemented|PostToolUse black auto-format + PreToolUse dangerous-bash guard in ~/.claude/settings.json; scripts in ~/.claude/hooks/; enforced at CC layer unconditionally
D062|browser-virtual-display|implemented|browser.py _ensure_virtual_display(): pyvirtualdisplay/Xvfb started when DISPLAY unset; browser tasks headless on server, never appear on user desktop

// ── Session 2026-03-14g additions ─────────────────────────────────────────────

D063|browser-use-gpt4o-mini|implemented|browser_use LLM=gpt-4o-mini via OR (avoids Anthropic integer-minimum schema rejection in 0.11.x/0.12.x); cheaper; BROWSER_USE_MODEL env var overrides
D064|db-proxy-sql-tracing|implemented|_DBContext uses set_trace_callback() to capture last SQL; slow query warnings include SQL snippet; dedicated db_queries.log with turn_id tie-back to forensic_logger
D065|inference-controller-auto-restart|implemented|_try_restart_local_ollama() in inference_gateway.py; always-on; 60s cooldown; called when is_healthy()=False; restart_ollama tool also available manually

// ── Session 2026-03-14h additions ─────────────────────────────────────────────

D066|pipe-trigger-word-boundary|implemented|basal_ganglia.py Format 1 pipe-separated triggers use \b regex word-boundary; prevents "hi" matching inside "this"; all habits benefit
D067|routing-directive-strip-from-core-input|implemented|thalamus.py strips [Routing directive:...] suffix from core_text; prevents CC bridge directive words ("background","inline") from triggering threshold habits
D068|code-ref-dispatch-uses-core-input|implemented|main.py habit code_ref dispatch passes parsed.core_input (routing directive stripped) not raw user_input to single-arg tools

// ── Session 2026-03-14i additions ─────────────────────────────────────────────

D069|reading-mode-sympathetic-autonomic|defined|cloud_ok field in queue items; "tonight"=background/local-only (cloud_ok=False); "now"=foreground/cloud-OK; encodes parasympathetic vs sympathetic at queue level; tonight drains overnight on local; now starts immediately with cloud if needed
D070|auto-reload-with-episodic-memory|implemented|G-SP1: self_edit._push_edit_episodic() creates EPISODIC memory (portable=False, source=self_edit); _try_hot_reload() calls it unconditionally; both callsites pass reason
D071|cloud-ok-runtime-switch|defined|cloud_ok lives in ~/.TheIgors/cloud_ok_override.json (TTL-based file); is_cloud_ok_override() in cloud_mode.py; inference_gateway checks per-call; env vars NOT used (can't change in running subprocess); two habits: PROC_SET_CLOUD_NOW (human trigger, writes override) + PROC_NIGHT_READ (threshold, clears override + drains local)

// ── Session 2026-03-14k additions ─────────────────────────────────────────────

D072|graph-first-response-vigilance|defined|G-MP1 architecture: inhibition happens before output in the graph layer (not after via evaluator LLM). predict_next() + gradient_flatness() on generation graph injected into _build_think_context() as [REFLEXIVE_PATTERN]. LLM self-inhibits obvious path. Cost=microseconds. Gate=IGOR_NPASS_REPLY. Biology basis: ART vigilance gate + attractor dynamics + Global Workspace Theory. TWM IS the narrative engine activation state — not two things. FLATNESS DIRECTION: gradient_flatness()=0.0 means STEEP (strong dominant prediction = REFLEXIVE); =1.0 means FLAT (no dominant prediction = novel/uncertain). LOW flatness = reflexive = inject warning. HIGH flatness = no graph data = D073 escalation signal.
D073|llm-escalation-is-graph-exhaustion|defined|We reach up to LLM only when graph traversal has genuinely exhausted — enough real time + passes + resonance cycles to honestly say "I don't know." Not a canned fallback. flatness() that never converges (stays LOW after many cycles) = genuinely stuck = escalate. Contrast: flatness HIGH immediately = reflexive = inhibit and re-traverse (D072). Factual queries: achievable soon. Fuzzy/generative queries: harder, deferred. Principle: "the program is the data" — the graph IS the knowledge; LLM is the trainer, not the primary answerer.

// ── Session 2026-03-15b additions ─────────────────────────────────────────────

D074|habit-intent-gate|implemented|basal_ganglia.select_habit(): skip habit_type="threshold" always (handled by ResourceMonitorSource/pre-submit hook); skip habit_type in (action+proactive with code_ref) + (workflow,delegation,reactive) when parsed_intent in _QUESTION_INTENTS={factual_question,meta_question,explanation_request,general,conversation}; prevents calendar/swap/ssh habits misfiring on question vocabulary overlap; G-OVN-1
D075|context-inject-habit-type|implemented|new habit_type="context_inject": fetches LTM identity+CP memories via cortex.search(), pushes SELF_CONTEXT to TWM (salience=0.75, ttl=90s), sets habit=None to fall through to LLM; LLM sees enriched context and answers introspectively; replaces canned WHO_AM_I response; G-HB3; main.py before if habit: block

// ── Session 2026-03-15c additions ─────────────────────────────────────────────

D076|designer-worker-cc-pattern|implemented|two persistent CC sessions: Designer (architecture+conversation+Akien relationship) + Worker (execution: loads files, runs code, writes results); shared queue ~/.TheIgors/cc_channel/queue.json; cc_queue.py CLI; WORKER_CONTEXT.md boot doc; Igor observer hook planned (T008)

// ── Session 2026-03-15d additions ─────────────────────────────────────────────

D077|ne-traversal-cursor|implemented|TraversalCursor dataclass tracks thread_topic across NE cycles; oscillation detection (same topic × NE_OSCILLATION_DEPTH + 0 promotions → reset thread); convergence (≥2 promotions); _cursor_context() injected into prompt; NE_MIN_INTERVAL_SEC=5; NE_CURSOR| snapshots to ring_memory; #236
D078|employer-model|implemented|Akien/Leah/Claude are peer employers of Igor; each gets master notebook in Igor DB; metadata.employer_id convention (no schema change); cortex.for_employer(); /api/cc_notebook GET/POST; Claude notebook seeded (4 entries); Leah+Claude already in genesis RM memories; #239
D079|decision-capture-habit|implemented|PROC_DECISION_CAPTURE + PROC_DECISION_CAPTURE_CC: passive_capture type; triggers on 'decided agreed plan confirmed going with'; stores as EPISODIC to LTM; D001 applied to dev process itself; #235
D080|watchlist-as-habit-subtree|defined|habit_type="watch" — fires salience boost instead of action; named inspectable list (Igor can answer 'what are you watching for?'); watch_expires in metadata (None=permanent); inward+outward facing entries; same BG scoring as all habits; #240
D081|executive-function-is-inspection-topology|defined|executive function emerges from inter-layer inspection topology, not a module; PFC analog = dense bidirectional inspection edges; each inspection edge is a graph node; build more inspection edges, not a controller; #242
D082|intrinsic-motivation-curiosity-state|defined|curiosity = idle state (low arousal + positive valence + open attractor) → habit fires to explore; NE internal_state → milieu reward signal on resolution; temporal credit assignment ('thanks past self') deferred; meaning-to-me level has highest milieu impact; #246

// ── Session 2026-03-15e additions ─────────────────────────────────────────────

D083|scribe-worker-three-session-pattern|implemented|three persistent CC sessions: Designer (architecture+Akien) + Implementation Worker (code) + Scribe Worker (memory coherence: DSB edits, Igor cc_notebook flushes, GitHub comments, commits); mirrors Igor's parallel focal points; flush_decision/flush_session in cc_queue.py; SCRIBE_CONTEXT.md boot doc; savestate skill reordered critical-first + Step 0.5 Igor flush; #249
D084|issue-filing-as-mini-savestate|implemented|each GitHub issue filing = mini savestate for that decision; decision not made until in Igor's memory; DSB commit = durable backup only; flush_decision wires this automatically
D085|training-curriculum-order|defined|Layer 1: Claude programming knowledge (organizational skeleton); Layer 2: Akien code+docs (lands on Layer 1, used natively); Layer 3: collaboration record (decisions_log+sessions — what no other Igor will have); order matters — Layer 2 without Layer 1 = stored but not organized
D086|igor-adaptive-friction-reducer|defined|three milieu-driven modes: high-energy=surface lever, medium=walk through steps, low=do-the-thinking+queue+surface-one-thing; data: writing patterns+Confluence+return frequency; Leah context with discretion; epic #251
D087|organizer-knowledge-base|defined|Igor researches Franklin/GTD/ADHD-productivity/motivational-science → high-inertia FACTUAL nodes; Akien annotates with personal experience; science+patterns+live-state = complete organizer substrate; #252
D088|superclaude-or-failover|implemented|balance-check at launch: OR via API, Anthropic via Igor browser_use→cc_channel/anthropic_balance.json; sources .env, reads anthropic_balance.json (24h TTL), falls over to OR when balance≤$10; logs every decision to ~/.TheIgors/logs/superclaude.log; check_claude_balance Igor tool added to browser.py; #253

// ── Session 2026-03-15f additions ─────────────────────────────────────────────

D089|headless-worker-script|implemented|claudecode/worker bash script: claude -p loop, one task per invocation, bash loops until queue empty, logs to ~/.TheIgors/logs/worker_role.log; no terminal needed; launch: nohup ~/TheIgors/claudecode/worker &
D090|scribe-batch-discipline|implemented|Scribe self-directs from task log (reads Worker done messages, derives what docs changed); one commit per session when queue empties; Implementation Worker never queues Scribe tasks; SCRIBE_CONTEXT.md + WORKER_CONTEXT.md updated

// ── Session 2026-03-15g additions ─────────────────────────────────────────────

D091|memory-store-epic|defined|consolidate all memory access concerns: cache, _MEM_COLS_NO_EMBED, boot scans, migration flags, habit trigger index; MemoryStore = data layer, Cortex = reasoning layer; mirrors db_proxy/inference_gateway consolidations; children #259-261 first, then design; #262

// ── Session 2026-03-15h additions ─────────────────────────────────────────────

D092|db-proxy-universal-gateway|implemented|db_proxy becomes universal storage gateway for ALL databases (memories, notebook, budget, word_graph); W1: no raw sqlite3.connect anywhere — every DB goes through DatabaseProxy instance; W2: ensure_index()+EXPLAIN-based usage tracking+_cc_index_registry table; foundation for LMDB migration (word/habit graph) without touching callers; #263

// ── Session 2026-03-15i additions ─────────────────────────────────────────────
D093|remote-instance-architecture|defined|distributed reading farm: each Windows box runs lightweight Igor instance; reads URLs locally via local Ollama; writes memories back through NetworkDatabaseProxy (POST /api/db on main port 8080); per-domain rate limiting replaces global cap; instance identity isolation (ring_memory instance_id, milieu per-instance, job heartbeat+stale reclaim); SQLite stays single-writer on main; foundation for Leah's instance; ~16 concurrent reads at full cluster; #263 (W1=#264, W2=#265, W3=#266, W4=#267)

// ── Session 2026-03-16a additions ─────────────────────────────────────────────
D094|cc-direct-habit-execution|implemented|POST /api/execute_habit live: habit_id + args dict, bypasses NLU/thalamus, multi-arg dispatch via explicit args; cc_session_YYYYMMDD.log (newest-first); server.py _igor_fn; main.py execute_habit(); GET /api/execute_habit/{id} discoverability; CC ops habits: CC_CHECK_PROCESS (pgrep -af), CC_RUN_BASH, CC_RUN_PYTHON; genesis PROC_RUN_BASH/PYTHON preserved; #268

// ── Session 2026-03-16c additions ─────────────────────────────────────────────
D095|lists-table|implemented|first-class DB table: list_name+item_key+item_value+ref_type+ref_id+instance_id("" global); cortex list_set/list_get/list_remove/list_all; seeded: Registry:Tags(5 tags), lists.projects(Project:TheIgors); CC habits: CC_GIT_LOG/CC_FIND_TICKETS/CC_LIST_PROJECTS; git_log+find_tickets+list_projects in runner.py; project_id→DB path lookup; #269

// ── Session 2026-03-16d additions ─────────────────────────────────────────────
D096|now-file-convention|implemented|pipeline_manager.py: write_state/get_state/list_states/clear_state; ~/.TheIgors/{instance}/{category}/{name}/{state}.now (transient, one at a time) or .txt (terminal, permanent); mtime IS the timestamp; IGOR_INSTANCE_ID env
D097|format-conversion-habits|implemented|converter.py: convert_text(text,from_format,to_format) tool; EN<->CSB<->DSB; templates in CONV:* PROCEDURAL memories (PROMPT_START/END markers); lists.conv fast-path; chunked for long inputs; seed_conv_graph.py seeds 6 pairs + CONV:ROOT hub
D098|identity-graph|implemented|PERSON:Akien/Leah/Claude as FACTUAL memories + IDENTITY:ROOT hub + lists.identity fast-path (list_get('lists.identity','Akien')→'PERSON:Akien') + interpretive edges (married_to, employs); traversal from ROOT verified; claudecode/seed_identity_graph.py
D099|twm-global-workspace|implemented|TWM = global workspace (Baars GWT): parent_obs_id on twm_observations for slot branching; TWM_MAX_SLOTS=7; NE comparison pass: action_pointer set intersection across slots; solo slots decayed at 0.7; twm_get_slots()+twm_decay_slot(); twm_set_attractor() keeps top MAX_SLOTS-1 on non-emergency path

// ── Session 2026-03-16g additions ─────────────────────────────────────────────
D100|live-salience-co-activation|implemented|salience is not stored — it's computed live: most-connected node across ALL active attractor slots IS most salient right now; co-activation density = working salience; urgency falls out for free (node in 6/7 slots = screaming); NE comparison pass (D099) is first sketch; full: walk all slots after each NE cycle, count node appearances, that count IS salience; no scratch table needed
D101|milieu-time-series|implemented|milieu is 3D+time: ring buffer of timestamped (valence,arousal,dominance) rows; current state = recency-weighted sum of rows; high-salience event → new row at NOW (not bump on old row); reactivation creates new row at NOW with original memory's emotional signature; gradient over time = trajectory (arousal climbing = loop detection); parent habit fires when arousal gradient exceeds slope threshold over N rows; use case: insecurity vs autonomy-respect slots loop → gradient detects → parent decays both + injects regulate observation; depends on D099

// ── Session 2026-03-16h additions ─────────────────────────────────────────────
// G-WG1 closed: word_graph _WordDocProxy.__len__ COUNT(DISTINCT word) 1900ms→1.3ms via wg_meta cache; incremental word_count maintained in index() via changes() after INSERT OR IGNORE wg_word_lang
// PROC_HEURISTIC_FIRST_RESPONSE fixed: habit_type response→context_inject (silent background consideration); Igor diagnosed this himself in live chat; self-repair path taught via CC bridge
// Slow query tool live: get_slow_query_report() surfaces top offenders; next target = memories activation_count sort (329x at p50=272ms)

// ── Session 2026-03-16i additions ─────────────────────────────────────────────
// G-MEM2 closed: cortex.py search() Phase 1 SELECT *→SELECT _MEM_COLS_NO_EMBED; drops 6KB embedding blob×300 rows per search call; idx_activation was fine — I/O was the bottleneck
// D096 implemented: pipeline_manager.py; .now=transient(one active)+.txt=terminal(permanent audit); mtime IS the timestamp; no redundant fields
// D097 implemented: converter.py convert_text tool + seed_conv_graph.py; CONV:ROOT hub + 6 PROCEDURAL memories with PROMPT_START/END templates; lists.conv fast-path; chunked for long inputs

// ── Session 2026-03-16j additions ─────────────────────────────────────────────
D102|igor-base|implemented|IgorBase: base class for all long-lived Igor components; GC _get_instance_names() finds enclosing namespace dict via gc.get_referrers(); per-class lazy _logger; time_it() context manager with log_threshold_ms; record_perf() writes to perf_{ClassName}.log (prepend); _perf_summary() p50/p95/p99/max; dump() __dict__ truncated; _get_caller(depth) via inspect.stack(); wired into: DatabaseProxy, Cortex, Thalamus, NarrativeEngine, Milieu, InferenceGateway, BasePushSource, WordGraph, JobManager, Igor
// G-WG2 closed: word_graph predict_next LRU cache (512 slots); key=(sorted words, lang, fetch_n); skips 29M-row GROUP BY+ORDER BY on repeated context; evict-half on overflow; cleared on every index() call; p95 2580ms→~0.1ms on cache hit
// G-WG3 closed: doc_count batching; _pending_doc_count accumulates; flush every 10 docs via _DOC_FLUSH_EVERY=10; _doc_count property returns db+pending; was 116x×320ms per index() run → near-zero
// db_proxy.py: _db_log owner= parameter; DatabaseProxy._record() passes self.get_name() → every slow query entry identifies which component generates it

// ── Session 2026-03-16k additions ─────────────────────────────────────────────
// G-EMB1 closed: memory_embeddings separate table implemented; cortex.py _init_db+_get_or_compute_embedding+_get_embeddings_batch+backfill+search Phase2 batch; migration ran: 5531 embeddings moved, memories.embedding NULLed, VACUUM 185MB→95MB
// G-WG4 closed: idx_wgc_covering ON wg_cooccur(word_a,word_b,score) added to _SCHEMA + applied to live word_graph.db; EXPLAIN confirms USING COVERING INDEX
D103|no-turn-pipeline|defined|pipeline is continuous always; "turn" is LLM API artifact; no commit points; high-salience events enter pipeline at any time and produce outputs; interval = pipeline stage latency not turn duration
D104|the-watcher|defined|high-salience search result arriving = new pipeline input event (not passive context); Watcher is a graph tree in same substrate (habits watching habits); Zen witness pattern — observes without suppressing; grows with experience; named session 2026-03-16l
// D104 elaborated 2026-03-16m: Igor's seed categories: Language (substrate), Self-modification (programming/AI/neurology/psychology), Culture; new root nodes NOT manually seeded — emerge from gradient accumulation of repeated co-occurrence patterns crossing density threshold; archive = low activation_count not deletion; promotion mechanism = word_graph cluster density scan → named root node deposit in cortex

// ── Session 2026-03-16m additions ──────────────────────────────────────────────
// Watcher design elaborated: seed roots Language/Self-modification/Culture; emergence via co-occurrence gradient; T-habit-audit-pipeline queued (early habits prevent pipeline, rewrite needed post D103/D104 impl)
// Blog post "Language of Optimization Narrative" deposited to Igor DB for learning

// ── Session 2026-03-17a additions ──────────────────────────────────────────────
D105|claude-bridge|implemented|persistent Claude API session (port 8082) for habit repair — shared+back channel; seeded with habits+logs+gaps on startup; survives Igor restarts; model=claude-sonnet-4-6 via REAL_ANTHROPIC_API_KEY; web UI dual-pane; PROC_CC_JOIN seeded; @reboot cron; bridge replies forwarded to Igor on shared channel
D106|habit-repair-term|defined|"habit repair" adopted as term for surgical habit calibration work (Igor's suggestion); "habit calibration" = the broader practice; session 2026-03-17a

// ── Session 2026-03-17b additions ──────────────────────────────────────────────
D107|foreground-reading-habits|implemented|PROC_READ_NOW + PROC_STOP_READING + PROC_QUEUE_FOR_INGESTION seeded; start_foreground_reading()+stop_foreground_reading()+_fg_reading_loop() in ebook_reader.py; sentence-by-sentence loop (n=1, 1s pause, stop flag, chapter-boundary soft prompt); "one moment please" interrupt-ack pattern; URL/path/paste/raw-text detection; queue path disambiguation

// ── Session 2026-03-17c additions ──────────────────────────────────────────────
D108|cross-platform-path-manager|implemented|global PathManager singleton (paths.py); IGOR_RUNTIME_ROOT + IGOR_INSTANCE_ID env overrides; all 54 hardcoded ~/.TheIgors refs replaced across ~32 files; first_start.py wizard (instance name + DB host prompts); igor bash script dynamic .env discovery; prerequisite for Windows round; epic #275
D109|multi-attention-center-reading|planned|extends D093: main attention center surfaces new memory in remote instances; remote instance picks up memory as task and executes (reading); external ebook_reader.py tools migrate to internal PROCEDURAL habits; all three platforms (akiendelllinux+yoga9i+windows) run as separate attention centers on same DB; DB optimization becomes critical at this scale; epic #276
D110|project-self-model-log-to-db|planned|Igor builds traversable graph of project itself (files/purpose/relationships/architecture as FACTUAL+interpretive nodes); logs move from flat files to DB table (timestamp/level/category/source/message/turn_id/instance_id); both Igor and Claude traverse same graph for context; PROC_CC_CONTEXT_SEED habit delivers Claude startup context via traversal not file reads; deepest self-model + fastest context load; epic #277

// ── Session 2026-03-16n additions ──────────────────────────────────────────────
// GitHub Actions installed: claude.yml (@claude mention trigger) + claude-code-review.yml (auto PR review); ANTHROPIC_API_KEY set as GH secret
// Convention: "commit" = add+commit+pull+push full cycle (matches gitcommitandpush script)
// Design notes queued for tomorrow: boredom=milieu-too-settled trigger → learn queue; reading metrics = real pages/hour for ingestion capacity planning
// PROC_TASK_SUPPRESS_STALE misfire noted: too broad, catches new assignments as stale — T-habit-audit-pipeline evidence
// On Intelligence (Hawkins) added to learn_queue — Igor requested it (not assigned)

// ── Session 2026-03-17d additions ──────────────────────────────────────────────
// G-DB1 closed: learner.py converted to DatabaseProxy singleton (_igor_db_proxy()); removed raw _rl_db(); list_absorbed_books+get_reading_list+add_to_reading_list+update_reading_status all use with _igor_db_proxy()() as conn; Calibre DB exempt (3rd-party external)
// G-NE1 closed: narrative_engine.py _consolidation_merge_pass() + _merge_cluster(); cosine clustering (threshold=0.85, min_cluster=3, window=10); metadata.occurrence_dates preserves source timestamps; originals deleted via cortex.delete_memory(); no gate (conservative defaults ARE the safety)
// G37 Phase 2 closed: IGOR_DUAL_WORD_GRAPHS + IGOR_NPASS_REPLY + IGOR_COMPREHENSION_SIGNAL all default true; generation graph trains on every Igor reply; flatness logged to cognition_metrics.log

// ── Session 2026-03-17e additions ──────────────────────────────────────────────
// G46 closed: source + context_of_encoding fields already in Memory + DB; callers were not populating them; fixed in main.py (source="interaction", context_of_encoding=f"intent=... valence=... arousal=... complexity=...") and NE _apply_output (source="narrative_engine", context_of_encoding=f"ne_run=... importance=... arousal=...")
// #252 closed: annotate_learning() tool in learner.py deposits EXPERIENTIAL node (source="user_annotated", confidence=0.95) via DatabaseProxy; seed_annotation_habit.py seeds PROC_ANNOTATE_LEARNING (13 trigger patterns, habit_type=action, code_ref=tools.learner:annotate_learning, inertia=0.30, parent_id=CP2)

D111|habit-author-filter|implemented|author_filter metadata field on habits gates scoring by input author; CC-only habits (CC_RUN_BASH) skip on human messages; author plumbed through _process/_process_inner to select_habit; basal_ganglia.py + main.py

// ── Session 2026-03-17g additions ──────────────────────────────────────────────
// Habit repair round 2: CC_RUN_BASH author_filter (D111); PROC_TASK_SUPPRESS_STALE+PROC_HEURISTIC_FITS_HERE passive_capture; PROC_QUEUE_FOR_INGEST trigger tightened; PROC_RESP_ON_IT 'please' removed; flush_habit_cache tool added to ops.py
// Design banked: search depth tiers (cortex.search depth gated by TWM attractor_weight); post-habit ack fork; savestate endgame (dissolves into continuous DB saves); skills decomposition

// ── Session 2026-03-17f additions ──────────────────────────────────────────────
// D108 implemented: paths.py PathManager singleton (IGOR_RUNTIME_ROOT + IGOR_INSTANCE_ID env overrides); all 54 Path.home()/.TheIgors refs replaced across ~32 files; first_start.py wizard (instance name default=wild_igor_YYYYMMDDHHMMSS, DB host default=127.0.0.1); igor bash script updated for dynamic .env discovery + wizard integration; full cutover identical to DatabaseProxy pattern; commits cda9ba0 + 63729e0
// D108 status: implemented (was planned)
