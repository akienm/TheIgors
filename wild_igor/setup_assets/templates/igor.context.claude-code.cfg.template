# igor.context.claude-code.cfg — context about Claude Code sessions (D319/D325).
# Location: ~/.TheIgors/${IGOR_INSTANCE_ID}/igor.context.claude-code.cfg
# Gitignored (privacy). Lets Igor know what Claude Code's current focus / mode is.

# Current Claude Code working mode (e.g. "design", "sprint", "review").
CONTEXT_CC_MODE=

# Active ticket or topic Claude Code is working on.
CONTEXT_CC_ACTIVE_TICKET=

# Trust level for Claude Code worker dispatch (D259).
CONTEXT_CC_TRUST=high
