# igor.credentials.cfg — Igor's API keys and secrets (D319).
# Location: ~/.TheIgors/${IGOR_INSTANCE_ID}/igor.credentials.cfg
# NEVER commit this file. Gitignored by *.credentials.cfg pattern.
# Igor is the intended reader; future: encrypt to Igor's key only (D325).

# OpenRouter API key (primary inference route for Igor — NOT for Claude Code).
OPENROUTER_API_KEY=${OPENROUTER_API_KEY}

# Anthropic API key routed through OpenRouter for Igor (IGOR_TIER5_ENABLED=false normally).
# Note: Claude Code uses a separate Anthropic account (see superclaude / CLAUDE.md).
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}

# Igor's SSH private key path (used for swarm machine access, not Akien's key).
IGOR_SSH_PRIVATE_KEY_PATH=${IGOR_SSH_PRIVATE_KEY_PATH}
