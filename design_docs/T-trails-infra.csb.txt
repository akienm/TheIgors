DOC|T-trails-infra|v2|date=2026-03-22
STATUS|design-complete|size=M|priority=9|ticket=T-trails-infra
RELATES|subsystem_memory.dsb, #289 (Hebbian training live)

ACTUAL_STATE|
  tails: 15,596 rows, 13,018 with trail_id. Migration DID run on Postgres.
  traces: 2,935 rows. columns: id, recorded_at, query, nodes(JSON).
  Hebbian training (_apply_trail_training) is wired and running.
  IGOR_TRAIL_TRAINING_ENABLED is the only gate — flip it to true once schema is clean.
  Original "phantom data" problem: RESOLVED. Design doc v1 was wrong about this.

ARCHITECTURE_CORRECTION|
  trail_activations table is NOT needed.
  tails IS the trail_activations table: node_id + weight + trail_id + sequence_pos.
  The distinction is temporal:
    tails = heat (decays, 7-day window, Hebbian signal)
    traces = provenance (permanent, query + result snapshot)
  Both already exist. The gap is traces missing the join/context columns.

REAL_WORK|
  1. Add 4 columns to traces table (all nullable TEXT):
     purpose      — 'search' | 'habit_trigger' | 'ne_consolidation' | 'interoception'
     twm_obs_id   — soft FK to twm_observations.id (which attractor drove this search)
     instance_id  — Igor instance identifier
     thread_id    — conversation thread

  2. Named migrations (add to _migrations guard):
     "traces_v2_purpose_twm"
     ALTER TABLE traces ADD COLUMN IF NOT EXISTS purpose TEXT;
     ALTER TABLE traces ADD COLUMN IF NOT EXISTS twm_obs_id TEXT;
     ALTER TABLE traces ADD COLUMN IF NOT EXISTS instance_id TEXT;
     ALTER TABLE traces ADD COLUMN IF NOT EXISTS thread_id TEXT;

  3. Wire twm_obs_id in _record_trace():
     At call site, look up active TWM attractor via twm_get_attractor().
     Pass obs_id as optional param. Store in traces row.
     Caller: cortex.search() already calls _record_trace() — add twm_obs_id lookup there.

  4. Set IGOR_TRAIL_TRAINING_ENABLED=true in .env — data is ready.

  5. Update _record_trace() return value to be consistent:
     Already returns trace_id (used as trail_id for tails). Confirm no None returns.

WHAT_DOES_NOT_CHANGE|
  - tails table schema: already correct
  - traces table data: don't touch existing rows (nullable columns, backfill not needed)
  - trail_metadata: this is just the traces table. No rename needed.
  - trail_activations: this is just the tails table. Don't create a duplicate.
  - Hebbian training code: already wired, just needs gate enabled

TWM_TRAIL_JOIN|
  traces.twm_obs_id links a search trail to the TWM attractor that drove it.
  Makes trails queryable by attractor: "which searches did this TWM slot trigger?"
  Enables future: attractor quality scoring (rich trail = useful attractor).

THREE_TEMPORAL_SYSTEMS|
  milieu: body baseline, slow drift (alpha=0.05 after T-interoception)
  TWM: attractor slots with urgency decay
  tails: per-node heat, 7-day window, Hebbian signal
  traces.twm_obs_id: links all three — a trail records which TWM attractor drove it,
  leaves heat in tails, and its timing is milieu-adjusted context.

SIZE_CORRECTION|M (was L in v1) — 4 ALTER TABLE migrations + 1 method wire + .env flip

INERTIA|MEDIUM — cortex.py changes; _record_trace() is low-inertia helper method

WORKER_READY|yes — Akien approved 2026-03-22
