# design_docs/archive/

Per-ticket and per-issue design files, moved here once the design phase is complete
or the ticket has been built. Kept for reference — not part of the active doc set.

Human-readable design docs live in `design_docs/` (the parent directory).
Igor-facing DSB docs live in `design_docs_for_igor/`.
