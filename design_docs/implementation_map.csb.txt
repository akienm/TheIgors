DOC_ID|implementation_map|v2026-03-01|status:living_document
purpose: file-by-file map of what lives where; key classes/functions; data flow
maintainer: claude_code|updated_after: each change group

[FILE_MAP]

FILE|wild_igor/igor/main.py|inertia:MEDIUM(0.50)|lines:~1100
  class Igor(instance_id)
    __init__: boots cortex+observer+reasoners+NE+pool+discord+net_listener+web_server+boot_check; _load_warm_context(); surfaces ring/restart/changelog/change_request
    run(): main event loop; 0.5s tick; stdin checked FIRST (responsiveness); then _drain_network + background + _drain_action_impulses
    get_stats() → dict: memory_count|session_cost|last_valence|last_friction|arbiter_pending (gateway pattern D015)
    _instance_dir() → Path: ~/.TheIgors/igor_{instance_id}/ — shared helper for warm_context + identity exports
    _process(user_input) → str: dashboard bookend + try/finally idle reset; delegates to _process_inner()
    _process_inner(user_input, is_impulse): routing signal detect → arbiter_intercept → thalamus → preparse → habit||reason → store → ring → dashboard → stamp _last_response_time
    _drain_network(): polls net_listener.incoming; routes by source; web replies via web_server.send()
    _drain_action_impulses(): reads TWM for NE action_impulses; is_irreversible_impulse() → arbiter or _process()
    _pre_compaction_flush(): summarize_session via Ollama → INTERPRETIVE memory + changes.log; fires once at URGENT_AT=30
    _run_ne_background(): fires NarrativeEngine in daemon thread; skips if still running
    _find_habit(parsed) → Memory|None: text-match trigger against PROCEDURAL memories
    _handle_command(command, raw): dispatches /commands
    _cmd_help|_cmd_memories|_cmd_core|_cmd_local|_cmd_model|_cmd_ollama
    _cmd_habits(raw): /habits list|pending|compile|explain N
    _cmd_arbiter(raw): /arbiter list|approve N|deny N|explain N
    _export_portable_identity(): writes SOUL.md (~/.TheIgors/) + IDENTITY.md (instance dir) from live DB; every boot (change.36)
    _announce_first_boot(): Discord ping + machines.csv self-registration on is_new (change.36)
    _cmd_compress: summarize ring→LTM via Ollama; sys.exit(42) to restart
    _cmd_quit|_cmd_restart|_shutdown(reason): calls _save_warm_context() before exit
    _arbiter_resolve(arbiter_queue, item_id, status): resolve + store EPISODIC learning memory (valence ±0.7)
    _save_warm_context(): serializes TWM+ring_tail+ne_state+session_summary to ~/.TheIgors/igor_{id}/warm_context.0.json; rotates .0→.1 first (D022)
    _load_warm_context(): loads .0 (fallback .1); TTL=WARM_CONTEXT_TTL_HOURS (def 4h); expired→archive; restores session_summary+ne_state always; ring_tail+TWM only if DB empty (D022)
  instance vars: _last_response_time(float)|_consecutive_slow(int) — Part C routing signal tracking
  _stdin_reader(stdin_queue): daemon thread; readline loop; pushes to queue
  _make_instance_id(host) → str: base34 epoch seconds
  main(): load .env; parse args; find/create instance_id; Igor().run()
  globals: DATA_DIR|CHANGE_LOG_PATH|CHANGE_REQUEST_PATH

FILE|wild_igor/igor/memory/models.py|inertia:HIGH(0.95)|lines:78
  class MemoryType(Enum): ROOT|CORE_PATTERN|IDENTITY|ROLE_MODEL|EPISODIC|PROCEDURAL|INTERPRETIVE|EXPERIENTIAL|FACTUAL
  dict BASE_INERTIA: type→float
  @dataclass Memory: id(8-char uuid)|narrative|memory_type|parent_id|children_ids|link_ids|valence|activation_count|friction_history|timestamp|metadata
    @property inertia() → float
    @property avg_friction() → float|None
    @property is_habit() → bool

FILE|wild_igor/igor/memory/cortex.py|inertia:MEDIUM(0.75)|lines:~420
  class Cortex(db_path)
    _init_db(): CREATE TABLE IF NOT EXISTS memories|ring_memory|twm_observations; ALTER TABLE memories ADD COLUMN embedding (idempotent migration)
    LTM: store|get|get_children|get_by_type|add_child|record_activation|search|count_by_type|total_count|get_habits|integrity_check
    ring: write_ring|write_restart_note|read_ring_memory|get_last_restart_note
    TWM: twm_push|twm_read|twm_count_unintegrated|twm_mark_integrated|twm_update_salience|twm_clear
    search(query, limit=10): hybrid — Phase1 text keyword scan → limit×2 candidates; Phase2 embed+cosine_sort; falls back to text-only if Ollama unavailable; sets m.relevance_score
    _get_or_compute_embedding(memory): DB lookup → if NULL, embed() + UPDATE memories SET embedding=?; lazy migration
    integrity_check() → (bool, str): verifies CP1-CP6 exist with correct parent relationships; run at boot; logs to ring(integrity_check)
  RING_MAX=50|TWM_MAX=50

FILE|wild_igor/igor/brainstem/core_patterns.py|inertia:HIGH(0.95)|lines:~260
  initialize_genesis(cortex, instance_id) → root_id
    plants: ROOT + CP1-CP6 + ID1-ID14 + RM_AKIEN/RM_LEAH/RM_CLAUDE/RM_IGOR_DW + PROC1-PROC10
    guard: if cortex.total_count()>0: skip (only on fresh DB)
  get_core_patterns(cortex) → list[Memory]
  validate_against_core(response, cortex) → (bool, str): Haiku semantic check against CP1-CP6; IMPLEMENTED (change.27)
    uses claude-haiku; checks for CP violations; returns (False, explanation) on violation
    caller (anthropic.py): logs to ring(ethics_gate) + submits to arbiter on failure
  BOOT_HASHES: dict of expected CP narrative sha256 values (change.29)
    verified at boot; mismatch → ring alert + Discord + refuse normal operation
  WRITE_SANDBOX: brainstem/ excluded from self_edit writes (enforced in self_edit.py, change.26)

FILE|wild_igor/igor/brainstem/amm_diagnostics.py|inertia:HIGH(0.95)|lines:194
  class SmartDict(OrderedDict): .bannerize(title)|.show(title)
  class Diagnostics(logging.Logger): debug|info|warning|error|critical with context prefix + bannerize
    .break_here(): drop into caller frame (pdb)
    @staticmethod _bannerize(data, title): pretty-print dicts/lists
    @classmethod requests_response_to_smartdict(response) → SmartDict
  get_logger(name) → Diagnostics
  STATUS: not wired into Igor's main path; standalone debug utility

FILE|wild_igor/igor/cognition/thalamus.py|inertia:MEDIUM(0.50)|lines:91
  @dataclass ParsedInput: raw|intent|keywords|tone|is_command|command
  process(raw_input) → ParsedInput
  _extract_keywords(text) → list[str]: stop-word filtered; 3+ char words
  _classify_intent(text, keywords) → str: regex/string matching; 7 categories
  _detect_tone(text) → str: keyword matching; 5 categories

FILE|wild_igor/igor/cognition/prefrontal_cortex.py|inertia:MEDIUM(0.75)|lines:155
  reason(user_input, relevant_memories, core_patterns, instance_id, reasoner) → (str, float)
  _log_judgment(cortex, judgment_type, inputs, result, reasoning): ring write category="judgment"
  assess_valence(interaction_text, response_text, cortex) → float[-1,1]: keyword pos/neg count
  measure_friction(used_api, retry_count, tool_failures, cortex) → float[0,1]: 0.25 per API call
  calculate_roi(goal_achieved, new_learning, used_api, cortex) → float[-1,1]

FILE|wild_igor/igor/cognition/narrative_engine.py|inertia:MEDIUM(0.75)|lines:~400
  NE_MODEL="gemma3:270M"|NE_TRIGGER_OBS=5|NE_MIN_INTERVAL_SEC=30|NE_MAX_INTERVAL_SEC=300|PROMOTE_THRESHOLD=0.7
  SOURCE_FILTER: NE filters source="narrative_engine" obs from all TWM reads (change.20a)
  SELF_DIAG_FILTER: "loop"|"stall"|"recursive"|"detecting own" → ring(ne_diagnostic) never LTM (change.20a.2)
  OBS_TOKEN_CAP: NE_MAX_OBS_CHARS=7200 (~1800 tokens); oldest dropped first (change.20a.3)
  class NarrativeEngine(cortex, instance_id)
    should_run() → (bool, str): timing + obs count; excludes NE-originated obs from meaningful-source check
    run(verbose) → dict|None: reads TWM (external obs only); caps obs; _call_ollama → _apply_output()
    _apply_output(result, obs_list): update salience; mark integrated; promote to LTM; write ring; push action impulses
    _call_ollama(prompt, max_twm_id) → dict|None: checks reasoning_cache first; writes cache on success
    _call_claude_fallback(prompt) → dict|None: Haiku fallback if Ollama fails + budget permits

FILE|wild_igor/igor/cognition/forensic_logger.py|inertia:LOW(0.30)|lines:~200
  LOG_DIR=~/.TheIgors/logs/|MAX_BYTES=10MB|all logs newest-first (prepend)
  log_reasoning_call(): reasoning_calls.log — every upstream API call
  log_ne_run(): ne_runs.log — every NE run or skip
  log_self_edit(): self_edit.log — every self-edit attempt (OK/BLOCKED/SYNTAX_FAIL)
  log_tool_call(): tool_calls.log — every tool invocation
  log_memory_op(): memory_ops.log — store/search/retrieve/update ops
  log_tier_selection(): reasoning_calls.log — tier decision before each upstream call; includes complexity_score+signals
  log_routing_decision(): reasoning_calls.log — local pool routing; est_latency|actual_latency|budget|cost_score|speed_score|tier_score|escalated|weights

FILE|wild_igor/igor/cognition/reasoning_cache.py|inertia:LOW(0.30)|lines:70
  CACHE_DIR=~/.TheIgors/cache/reasoning/|TTL_SECONDS=720 (12 min)
  get(model, prompt, current_max_twm_id) → str|None: returns cached text; None if TTL expired or new TWM obs since write
  put(model, prompt, response_text, max_twm_id) → None: writes {response_text, ts, max_twm_id, model} as JSON
  invalidation: TWM watermark — if current_max_twm_id > stored max_twm_id → cache stale
  use_case: NE Ollama passes; skips repeated call when TWM unchanged in past 12 min

FILE|wild_igor/igor/cognition/embedder.py|inertia:LOW(0.30)|lines:69
  EMBED_MODEL="nomic-embed-text"|CACHE_DIR=~/.TheIgors/cache/embeddings/
  embed(text, model=EMBED_MODEL) → list[float]|None: SHA-256 file cache then ollama.embeddings(); None if Ollama unavailable
  cosine_similarity(a, b) → float: pure Python; no numpy; returns 0.0 on empty/mismatched

FILE|wild_igor/igor/cognition/push_sources.py|inertia:MEDIUM(0.75)|lines:~320
  class BasePushSource: push(cortex) → list[int]
  class MemorySurfacer: MIN_INTERVAL=120s; reads ring→keywords→LTM search→twm_push(sal:0.3-0.6,ttl:600)
  class HeartbeatSource: MIN_INTERVAL=300s; checks budget+arbiter+inbox+HEARTBEAT procedural memories → twm_push(sal:0.4,ttl:600); Discord alert on CRITICAL/EXHAUSTED budget (change.31; renamed from TimerSentinel)
  class UserInputSource: no timer; push_message(cortex,content,channel,author) → twm_push(sal:0.7,ttl:1800)
  class MachinesWatcher: CHECK=30s; mtime watch on machines.csv → twm_push(sal:0.8,ttl:3600)
  class InboxWatcher: CHECK=5s; new files in inbox dir → twm_push(sal:0.9,ttl:3600)
  singletons: memory_surfacer|heartbeat_source|user_input_source|machines_watcher|inbox_watcher
  run_background_sources(cortex) → int: calls all timer-based sources; swallows exceptions

FILE|wild_igor/igor/cognition/interruptors.py|inertia:MEDIUM(0.75)|lines:190
  class BaseInterruptor: check(cortex) → str|None; _write_alert(cortex, msg)
  class BudgetInterruptor: name="claude_budget"; checks budget_status(); 3 alert levels
  class ContextInterruptor: name="context_length"; WARN_AT=20|URGENT_AT=30|COOLDOWN=5
    _count_session_interactions(cortex): walks ring backward from SESSION_START marker
  ACTIVE_INTERRUPTORS: [BudgetInterruptor(), ContextInterruptor()]
  run_all(cortex) → list[str]: runs all; swallows exceptions

FILE|wild_igor/igor/cognition/local_pool.py|inertia:MEDIUM(0.75)|lines:~260
  MACHINES_CSV=~/.TheIgors/local/machines.csv|OLLAMA_PORT=11434|BENCHMARK_DIR=~/.TheIgors/benchmarks/
  BENCHMARK_TTL_HOURS=24|_DEFAULT_LATENCY_BUDGET=8.0
  PRIORITY_ORDER: {priority.realtime:0, priority.main_loop:1, priority.background:2, priority.batch:3}
  _BENCH_PROMPTS: 3 short fixed prompts for throughput measurement
  _parse_online_machines() → list[dict]: reads CSV; skips OFFLINE; sorts by priority
  parse_capabilities(row) → list[str]: splits Capabilities column
  Part A — benchmarking:
    _run_benchmark(model, host=None) → dict: runs 3 prompts; records tokens_per_sec+avg_latency_ms+measured_at; raises on failure
    _load_benchmark_cache(hostname, model) → dict|None: loads JSON if <24h old and model matches
    _save_benchmark_cache(hostname, result): writes ~/.TheIgors/benchmarks/{hostname}.json
    _init_benchmark_async(model, on_complete): loads cache or spawns daemon thread; calls on_complete(result) when ready
  class RoutingWeights: (Part B+C)
    __init__: reads ROUTING_WEIGHT_COST(0.60)+ROUTING_WEIGHT_SPEED(0.40) from env; CLAMP [0.2,0.8] STEP 0.05
    adjust(signal): "speed_pressure" → ↑speed ↓cost; "cost_pressure" → ↑cost ↓speed; "quality_pressure" → no-op
    score_local(est_latency, budget) → (cost_score, speed_score, tier_score)
  class LocalOllamaPool(model=DEFAULT_MODEL)
    __init__: _refresh(); _init_benchmark_async(); self.weights=RoutingWeights()
    _on_benchmark_done(result): stores into self._benchmark
    _estimate_latency(user_input) → float|None: prompt_tokens/tokens_per_sec; None if no benchmark yet
    reason(): estimate latency → if >budget raise (escalate to cloud); else round-robin; logs log_routing_decision() on both paths
    set_model(name): invalidates benchmark if model changed; triggers re-benchmark
    machines_summary() → str: shows hostname(priority)[caps] + bench tok/s

FILE|wild_igor/igor/cognition/observer.py|inertia:LOW(0.30)|lines:~80
  _cortex: module-level ref; set by init(cortex) at boot
  init(cortex): wires cortex for DB writes; called in Igor.__init__()
  observe(label, value, context=None): fire-and-forget metric write; never raises
    layer 1: ~/.TheIgors/logs/metrics.log (forensic; always on; 10MB rotation)
    layer 2: cortex.store(EXPERIENTIAL memory) with salience=0.2 (if cortex wired in)
    auto-detects caller module via inspect.stack()[1]

FILE|wild_igor/igor/cognition/reasoners/base.py|inertia:HIGH(0.90)|lines:~100
  abstract class BaseReasoner: reason()|name()
  token economy constants (D021):
    TOOL_RESULT_MAX_CHARS=20_000 | MAX_TURNS=25 | CONTEXT_WARN_CHARS=100_000
  @staticmethod _cap_tool_result(result) → str: truncates + appends [TRUNCATED — N chars...] notice
  @staticmethod _messages_total_chars(messages) → int: rough char count of all message content

FILE|wild_igor/igor/cognition/reasoners/anthropic.py|inertia:MEDIUM(0.70)|lines:~370
  DEFAULT_MODEL="claude-sonnet-4-6"|DEBUG_BYPASS_MODEL="claude-haiku-4-5-20251001"
  MODEL_ALIASES: sonnet/opus/haiku → full IDs
  SELF_EDIT_TRIGGER_TOOLS: {read_source_file, list_source_files}
  class AnthropicReasoner(BaseReasoner)
    reason(): tool loop; MAX_TURNS guard; CONTEXT_WARN_CHARS warning; auto-Haiku on self-edit detect; budget check per turn; _cap_tool_result() on all tool outputs; interruptors after final
    ethics_gate: validate_against_core() on every final response; FAIL → ring(ethics_gate) + arbiter.submit(action_type=ethics_flag)
    _estimate_cost(usage, model) → float

FILE|wild_igor/igor/cognition/reasoners/ollama_reasoner.py|inertia:MEDIUM(0.70)|lines:~300
  DEFAULT_MODEL="gemma3:1b"
  _LOG_PATH: wild_igor/ollama_calls.log
  class OllamaReasoner(model, host): reason() → (str, 0.0); no tools; host=None → localhost
  preparse(user_input, habits, model) → dict: JSON classification via Ollama
  score_memories(query, memories, model, top_n=5) → list[Memory]: 0-10 scoring via Ollama
  summarize_session(ring_entries, instance_id, model) → str: labeled-field template (TASKS/CHANGES/TOOLS/STATE/VALENCE) for reliable gemma3:1b output
  compute_complexity(user_input) → dict: pure-Python heuristic; signals verb_match/scope_match/multi_step/multi_tool; score>0.6 → skip_to=tier.4

FILE|wild_igor/igor/memory/scrub.py|inertia:MEDIUM(0.75)|lines:~60
  scrub(text) → str: regex-based credential scrubber; removes prefixed keys (sk-/Bearer/api_key=), hex≥32, base64≥40
  hooked into: cortex.store()|cortex.write_ring()|cortex.twm_push()|anthropic.py before messages|openrouter_reasoner.py before messages
  issue #19 fix (2026-03-01)

FILE|wild_igor/igor/arbiter/__init__.py|lines:0
  empty package init

FILE|wild_igor/igor/arbiter/queue.py|inertia:LOW(0.30)|lines:~220
  QUEUE_FILE=~/.TheIgors/igor_wild_0001/arbiter/pending.json (file-backed, not DB)
  IRREVERSIBLE_KEYWORDS: {send,delete,publish,purchase,email,message,remove,modify,post,notify,alert,deploy,write_file,execute,broadcast}
  @dataclass ArbiterItem: id|timestamp|action_type|description|context|threshold_reason|status|cost_estimate|metadata|resolution|resolved_at|resolution_note
  submit(description,context,action_type,threshold_reason,cost_estimate,metadata) → int: appends to JSON; _ping_discord() once per item
  resolve(item_id, status, note) → ArbiterItem|None: marks approved/denied; rewrites JSON
  get_pending() → list[ArbiterItem]|get_all()|get_item(id)|count_pending() → int
  is_irreversible_impulse(content) → bool: keyword scan
  _ping_discord(): one ping per new item via discord_bot.send(); no spam
  tool: arbiter_submit registered in registry via tools/__init__.py import

FILE|wild_igor/igor/boot_check.py|inertia:MEDIUM(0.75)|lines:~90
  daemon thread started in Igor.__init__() after web_server.start()
  reads machines.csv; checks each online machine for nomic-embed-text + gemma3:27b via Ollama API
  pulls missing models (ollama pull); logs CSB entries to changes.log; writes summary to ring(hardware)
  timeout: 5s probe, 600s pull; never blocks startup

FILE|wild_igor/igor/network/listener.py|inertia:MEDIUM(0.75)|lines:189
  @dataclass NetworkMessage: source|content|author|reply_info|raw
  incoming: queue.Queue  # unified; all sources → Igor
  _poll_discord()|_poll_web()|_poll_gmail()
  _run_loop(): every 0.5s: poll_discord+poll_web; every 300s: poll_gmail
  start(): launches daemon thread "network-listener"

FILE|wild_igor/igor/network/discord_bot.py|inertia:MEDIUM(0.75)|lines:208
  @dataclass DiscordMessage: content|author|channel_id|channel_name|guild_name|message_id
  incoming: queue.Queue|outgoing: queue.Queue
  send(channel_id, text): enqueues to outgoing
  class IgorBot(discord.Client): on_ready|on_message; drains outgoing on heartbeat
  start(): launches daemon thread; asyncio.run in thread

FILE|wild_igor/igor/web/server.py|inertia:LOW(0.30)|lines:407
  INBOX_DIR=~/.TheIgors/igor_wild_0001/inbox|OUTBOX_DIR=~/.TheIgors/igor_wild_0001/outbox
  incoming: queue.Queue  # web→Igor
  _client_queues: list[asyncio.Queue]  # per-WS; fanned out by _broadcast()
  _loop: asyncio.EventLoop  # captured in on_startup; used for call_soon_threadsafe
  _stats_fn: callable|None  # set by start(stats_fn=); called by /api/dashboard (gateway pattern D015)
  send(text): broadcast JSON {type:message,author:igor,...}
  _broadcast(payload): fan out to all _client_queues
  route handlers: _index|_api_upload|_api_outbox_list|_api_outbox_download|_api_dashboard|_ws_endpoint
  start(stats_fn): launches daemon thread "web-server"; stats_fn=Igor.get_stats (no cortex ref in server)
  fallback_html: shows ⚠ arbiter:N when pending > 0

FILE|wild_igor/igor/dashboard/terminal.py|inertia:LOW(0.30)|lines:180
  render(cortex, instance_id, ...): Rich Panel; alerts top; memory counts; metrics
  print_activated_memories|print_habit_trigger|print_reasoning
  _get_active_alerts(cortex): reads ring category="interruptor"; deduplicates by name
  _get_budget_line(): reads budget_status(); color-codes

FILE|wild_igor/igor/tools/registry.py|inertia:LOW(0.30)|lines:84
  @dataclass Tool: name|description|parameters(JSON Schema)|fn
    .execute(**kwargs)|.to_anthropic_schema()|.to_openai_schema()|.to_text_description()
  class ToolRegistry: register|get|all|execute|to_anthropic_schemas|to_openai_schemas|to_text_descriptions
  registry = ToolRegistry()  # module-level singleton

FILE|wild_igor/igor/tools/__init__.py|lines:~10
  imports: filesystem|web_search|self_edit|gmail|discord|senses|runner|confluence|budget|arbiter.queue
  NOTE: import triggers tool registration as side effect; arbiter.queue registers arbiter_submit

FILE|wild_igor/igor/tools/filesystem.py|WORKSPACE=/home/akien/
  read_file(path)|write_file(path,content)|list_directory(path)
  sandbox: _safe_path() rejects escapes from /home/akien/
  PDF support: pypdf; for .pdf files only

FILE|wild_igor/igor/tools/web_search.py
  web_search(query, max_results=5): DuckDuckGo HTML; no API key; returns titles+URLs+snippets
  read_webpage(url): requests+BeautifulSoup; returns text content

FILE|wild_igor/igor/tools/self_edit.py|SANDBOX=wild_igor/igor/
  INERTIA map: brainstem/:0.95|memory/models.py:0.95|reasoners/base.py:0.90|memory/cortex.py:0.75|...tools/:0.30
  WRITE_EXCLUDED={"brainstem/"}: Igor may READ but not WRITE these paths (change.26)
  blocked writes → logged to ~/.TheIgors/igor_wild_0001/blocked_edits.log + ring + return BLOCKED_EDIT error
  list_source_files(path)|read_source_file(path)|edit_source_file(path,content,reason)|patch_source_file(path,old,new,reason)|run_syntax_check(path)
  _git_commit_and_push(rel_path, reason): stage+commit+push on every edit; message="self-edit: igor/path"

FILE|wild_igor/igor/tools/budget.py
  DEFAULT_BUDGET_USD=10.00|WARN_FRACTION=0.20|CRITICAL_USD=2.00
  DB: memory/claude_budget.db; tables: spend|config
  get_budget()|set_budget()|get_spend_total()|get_remaining()|record_spend()|budget_status()|check_before_call()
  tools: check_claude_budget|set_claude_budget|claude_spend_history

FILE|wild_igor/igor/tools/gmail.py
  send_email(to,subject,body)|read_email_inbox(max_messages): SMTP+IMAP; app password; GMAIL_USER+GMAIL_APP_PASSWORD

FILE|wild_igor/igor/tools/discord.py
  send_discord_message(channel_id, text): wraps discord_bot.send()

FILE|wild_igor/igor/tools/senses.py|WORKSPACE=wild_igor/workspace/
  get_datetime(): TZ from env|take_photo(filename,device_index): OpenCV → workspace/
  list_cameras(max_index=5)|record_audio(seconds=5,filename): sounddevice+scipy → WAV

FILE|wild_igor/igor/tools/runner.py|WORKSPACE=wild_igor/workspace/
  run_bash(command, timeout=30)|run_python(code, timeout=30)
  NOT sandboxed; runs as current OS user; cwd=workspace/

FILE|wild_igor/igor/tools/confluence.py
  Atlassian Confluence read/search tools
  credentials: env vars (pattern matches gmail)
  STATUS: imported in tools/__init__.py — available to Igor (change.24)

FILE|wild_igor/igor/perception/__init__.py|lines:0
  STATUS: empty stub; no implementation

FILE|wild_igor/web_ui/|Svelte 4 + Vite frontend
  package.json: @sveltejs/vite-plugin-svelte|vite|svelte (devDeps)
  vite.config.js: dev proxy /ws+/api → localhost:8080; build outDir=dist
  src/App.svelte: root; Chat+Dashboard layout; flexbox column
  src/lib/Chat.svelte: WS connect; message list; drag-and-drop (svelte:window events)
  src/lib/Dashboard.svelte: polls /api/dashboard every 5s; shows mem/cost/val/friction/arbiter_pending
  src/lib/FileZone.svelte: visual drop overlay only
  build: npm install && npm run build → dist/ served by server.py

[DATA_FLOW]

FLOW|user_message (REPL)
  stdin → _stdin_reader → stdin_queue → Igor.run() [stdin checked FIRST] → Igor._process()
  → arbiter_intercept [if pending + approve/deny word] || thalamus.process()
  → [preparse(Ollama)] → [habit_match || pfc.reason(Anthropic||Ollama)]
  → console.print(response) → cortex.store(EPISODIC) → cortex.write_ring() → dashboard.render()

FLOW|user_message (web)
  browser → WS /ws → web/server.py _ws_endpoint → web_server.incoming.put()
  → network/listener.py _poll_web() → net_listener.incoming.put(NetworkMessage source=web)
  → Igor._drain_network() → Igor._process(synthetic) → web_server.send(response)
  → _broadcast() → all WebSocket clients

FLOW|user_message (discord)
  Discord API → discord_bot IgorBot.on_message() → discord_bot.incoming.put()
  → listener._poll_discord() → net_listener.incoming → Igor._drain_network()
  → Igor._process() [no web_server.send for discord; uses discord_bot.send() via tool]

FLOW|TWM_cycle
  push_sources|interruptors → cortex.twm_push()
  → NarrativeEngine.run() [background thread, every 30s-5min]
  → twm_read() → reasoning_cache.get() [cache hit → skip Ollama] || _call_ollama() + cache.put()
  → _apply_output()
  → twm_mark_integrated() + twm_update_salience() + cortex.store(Memory) [if importance≥0.7]
  → cortex.write_ring(NE narrative fragment)

FLOW|action_impulse
  NE._apply_output() → twm_push(source=NE, type=action_impulse, ttl=300s)
  → Igor._drain_action_impulses() [per tick]
  → is_irreversible_impulse()? → arbiter.submit() + Discord ping [awaiting akien approval]
  → else → Igor._process(synthetic) [NE acts directly]

FLOW|arbiter_approval
  akien types "approve"/"approved"/"yes" (or /arbiter approve N)
  → _process() arbiter_intercept → _arbiter_resolve(oldest_pending, "approved")
  → arbiter.resolve() + EPISODIC memory(valence=+0.7) [learning]

FLOW|cortex_search
  cortex.search(query) → Phase1 text keyword scan → candidates
  → embed(query) + _get_or_compute_embedding(each_candidate)
  → cosine_similarity ranking → returns sorted list with m.relevance_score set
  [falls back to Phase1 order if Ollama unavailable]

FLOW|self_edit
  Igor detects edit intent → AnthropicReasoner auto-switches to Haiku
  → read_source_file() [reads current state] → patch_source_file(old,new,reason)
  → brainstem/ write check [BLOCKED if path in WRITE_EXCLUDED]
  → ast.parse() [syntax check] → write file → _git_commit_and_push() → restart required

[KEY_PATHS]
config: wild_igor/.env|wild_igor/.env.example
machines: ~/.TheIgors/local/machines.csv  # now has Priority + Capabilities columns
soul: ~/.TheIgors/SOUL.md                  # CP1-CP6 export; refreshed every boot
identity: ~/.TheIgors/igor_{id}/IDENTITY.md # ID1-ID14 export; instance-specific
instance_data: wild_igor/data/{instance_id}.db
inbox: ~/.TheIgors/igor_wild_0001/inbox/
outbox: ~/.TheIgors/igor_wild_0001/outbox/
arbiter_queue: ~/.TheIgors/igor_wild_0001/arbiter/pending.json
blocked_edits: ~/.TheIgors/igor_wild_0001/blocked_edits.log
changes: ~/.TheIgors/claudecode/changes.log
change_requests: ~/.TheIgors/claudecode/change_request.txt
budget_db: wild_igor/memory/claude_budget.db
embedding_cache: ~/.TheIgors/cache/embeddings/
reasoning_cache: ~/.TheIgors/cache/reasoning/
benchmarks: ~/.TheIgors/benchmarks/{hostname}.json  # per-machine Ollama throughput; 24h TTL
warm_context: ~/.TheIgors/igor_{id}/warm_context.{0,1}.json  # warm working memory; 4h TTL; rotation .0→.1
metrics_log: ~/.TheIgors/logs/metrics.log  # observe() forensic layer
ollama_log: wild_igor/ollama_calls.log
discord_log: wild_igor/logs/discord.log
web_ui_dist: wild_igor/web_ui/dist/
workspace: wild_igor/workspace/
